server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # SPA: cualquier ruta cae en index.html (React maneja el routing).
  location / {
    try_files $uri $uri/ /index.html;
  }
}
